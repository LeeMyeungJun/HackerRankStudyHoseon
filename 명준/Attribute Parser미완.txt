#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    int iLine = 0;
    int iValue = 0;
    int iCenter = iLine /2;
    cin>>iLine>>iValue;
    
    vector<string> vectorArr(iLine);
    vector<string> vectorValue(iValue);

    int i = 0;
    int iIndex = 0;
    for(i = 0 ; i < iLine ; i++)
    {
        cin>>vectorArr[i];
    }

    for(i = 0 ; i < iValue ; i++)
    {
        cin>>vectorValue[i];
    }

    for(i = 0 ; i <iValue ; i++)
    {
        iIndex = vectorValue[i].find('~'); //인덱스번호로 넘어옴 
    }
   


    return 0;
}
